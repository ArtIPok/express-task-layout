<!DOCTYPE html>
<html lang="en" dir="ltr">
    <h2>Contact me</h2>
    {{#if isSent}}
        <div class="isSent">
            <p>The message has benn sent!<br>
            File {{ files.file.name }} has benn saved!</p>
        </div>
    {{/if}}
    {{#if isError}}
        <div class="isError">
            You can't leave fields empty
        </div>
    {{/if}}
    <form action="/contact/send-message" method="POST" enctype="multipart/form-data">
        <label>
            Your name: <input type="text" name="author" autoComplete="off">
        </label>
        <label>
            Your email: <input type="email" name="sender" autoComplete="off">
        </label>
        <label>
            Title: <input type="text" name="title">
        </label>
        <label>
            Project design: <input type="file" name="file" accept=".png, .jpg, .jpeg, .gif" autocomplete="off">
        </label>
        <label>
            Message: <textarea name="message"></textarea>
        </label>
        <button type="submit">Send message</button>
    </form>
</html>